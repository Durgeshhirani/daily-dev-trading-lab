info:
  name: express req info
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/request-info
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
