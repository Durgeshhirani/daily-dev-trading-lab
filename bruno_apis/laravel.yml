info:
  name: laravel
  type: http
  seq: 6

http:
  method: POST
  url: https://bsbprimedemo.bsbinfotrade.com/api/sellerFaqList
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
