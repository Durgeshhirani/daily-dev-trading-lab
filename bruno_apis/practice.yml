info:
  name: practice
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
